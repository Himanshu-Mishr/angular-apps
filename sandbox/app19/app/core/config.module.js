(function(){
	'use strict';

	/*
		contains configuration that is required by the all the modules of the app.
	 */
	angular.module('app.config')
		.config(autoAtStart)
		.config(setInitData);

		function autoAtStart() {

		}

		function setInitData() {

		}
})();